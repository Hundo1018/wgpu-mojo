# Symbols this binding resolves that wgpu-native v29.0.0.0 exports as
# unimplemented!() stubs. They link, so scripts/check-symbols.sh's resolution
# check passes — but CALLING ONE PANICS IN RUST AND ABORTS THE PROCESS, with no
# Mojo-level error to catch.
#
# This file is a ratchet, not an endorsement: it records the stubs already bound
# so the gate can fail on any NEW one. Everything listed here is a landmine for
# callers and should either be removed or guarded. See docs/BINDING_ROADMAP.md.
#
# Re-derive the current set with: pixi run check-symbols

# --- SetLabel: unimplemented for every object type. wgpu-mojo exposes
# --- set_label() wrappers over these; tests/test_debug_groups.mojo already
# --- has test_encoder_set_label() commented out of main() for this reason.
wgpuBindGroupLayoutSetLabel
wgpuBindGroupSetLabel
wgpuBufferSetLabel
wgpuCommandEncoderSetLabel
wgpuComputePassEncoderSetLabel
wgpuComputePipelineSetLabel
wgpuDeviceSetLabel
wgpuPipelineLayoutSetLabel
wgpuQuerySetSetLabel
wgpuQueueSetLabel
wgpuRenderBundleEncoderSetLabel
wgpuRenderBundleSetLabel
wgpuRenderPassEncoderSetLabel
wgpuRenderPipelineSetLabel
wgpuSamplerSetLabel
wgpuShaderModuleSetLabel
wgpuTextureSetLabel
wgpuTextureViewSetLabel

# --- Buffer: mapped-range access and map-state query
wgpuBufferGetMapState
wgpuBufferReadMappedRange
wgpuBufferWriteMappedRange

# --- Async pipeline creation. BINDING_STATUS.md lists these as done; they are
# --- bound, but the underlying implementation does not exist upstream.
wgpuDeviceCreateComputePipelineAsync
wgpuDeviceCreateRenderPipelineAsync

# --- Device / instance queries
wgpuDeviceGetAdapterInfo
wgpuDeviceGetLostFuture
wgpuInstanceWaitAny
wgpuInstanceGetWGSLLanguageFeatures
wgpuInstanceHasWGSLLanguageFeature

# --- Shader compilation info (also listed as done in BINDING_STATUS.md)
wgpuShaderModuleGetCompilationInfo
