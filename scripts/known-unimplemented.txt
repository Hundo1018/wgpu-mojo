# Symbols this binding resolves that wgpu-native v29.0.0.0 exports as
# unimplemented!() stubs. They link, so scripts/check-symbols.sh's resolution
# check passes — but CALLING ONE PANICS IN RUST AND ABORTS THE PROCESS, with no
# Mojo-level error to catch.
#
# This list is EMPTY, and should stay that way. The 29 entries it was seeded
# with were removed in full (loader methods, public wrappers, C bridge paths)
# rather than documented — see docs/BINDING_ROADMAP.md "Tier 0". The file
# remains as the ratchet: check-symbols.sh fails if any binding resolves a stub
# that is not listed here, so re-introducing one is a deliberate act.
#
# Re-derive the library's current stub set with: pixi run check-symbols
